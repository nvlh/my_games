# 模拟器接入方案

## 当前状态

本轮已完成平台标签和本地 ROM 导入入口。导入只会在浏览器会话中记录文件名、平台判断和文件大小，尚未把 ROM 交给真正的模拟器核心运行；因此界面不会误导用户认为 SS 或 PSP 已经可以直接启动。

## 分阶段实现

| 阶段 | 目标 | 说明 |
|---|---|---|
| GBA | 首个可运行平台 | 先接入成熟的 WebAssembly GBA 核心，处理 `.gba`，验证键盘、触控、手柄、存档和移动端性能 |
| 土星 SS | 桌面端优先 | 需要评估核心、BIOS、`.cue/.bin` 或镜像组合、音视频同步和较高的 CPU/GPU 负载；移动端不承诺满速 |
| PSP | 高性能实验阶段 | 需要单独的 PSP WebAssembly 核心与更严格的设备性能检测，优先支持桌面 Chrome/Edge，暂不保证手机端 |

## 安全与合规

网站只读取用户自行选择且拥有合法权利的 ROM/BIOS 文件，不提供 ROM 下载，不将 ROM 打包进项目，不自动抓取第三方游戏资源。跨设备保存 ROM、封面和存档需要用户登录、文件存储、容量限制、删除入口与授权声明。

## 推荐技术路径

优先评估 EmulatorJS/RetroArch WebAssembly 核心的嵌入方式；其官方文档展示了通过 `EJS_core` 与 `EJS_gameUrl` 加载游戏，并说明现代浏览器可运行 WASM 核心。正式接入时应将每个平台封装为独立适配器，避免把 SS/PSP 的 BIOS 与格式差异混在统一上传逻辑中。
