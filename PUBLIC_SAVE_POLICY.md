# 匿名公共存档策略

当前网站不要求账号，所有访客按平台和游戏共享多条公共存档记录。每条记录拥有独立 ID、名称、创建时间、更新时间和版本；新建记录默认使用当前时间命名，用户可以重命名，也可以选择覆盖当前记录。数据库只保存平台、游戏标识、记录 ID、对象存储键、大小、版本、名称和时间字段；存档快照内容保存在对象存储中。ROM 文件不因保存存档而自动上传或托管。

每个存档快照限制为 2 MB，单个槽位服务端写入冷却为 3 秒；保存时可携带当前版本号，若数据库版本已变化则拒绝旧页面覆盖，用户需要先刷新再保存。前端提供新建存档、记录列表、时间显示、重命名、覆盖确认和下载快照后恢复游戏状态的入口。

当前不启用自动清理任务。原因是公共存档没有账号归属，自动删除可能误删其他访客使用的进度；如未来需要清理，应先增加管理员确认、保留期限、审计记录和对象存储生命周期规则，再单独实施周期任务。未引用的旧对象不会在本版本中主动删除，以避免误删和破坏读档。

本策略不代表已接入 GBA、土星 SS 或 PSP 的真实第三方模拟器核心；当前保存/恢复已接入 Babylon 原创原型，真实 ROM 模拟器应在获得合法核心与用户授权 ROM 后单独适配。
