# 公开游戏管理与上传说明

“游戏管理”入口允许访客提交合法授权的游戏文件、封面和截图。当前入口是公开的，不要求登录；因此不应上传私人文件、未授权 ROM 或 BIOS。项目只保存文件索引与元数据在数据库，文件字节保存在对象存储。

## 分类字段

平台支持：小霸王、街机、NES、GBA、WSC / WonderSwan Color、土星 SS 和 PSP。类型支持：动作、射击、平台跳跃、格斗、竞速、益智、角色扮演、冒险、体育和其他。每条记录还保存游戏名称、英文标识、简介、玩家数和操控方式。

## 对象存储目录

上传后由服务器自动写入以下逻辑目录，文件名会经过安全清理并追加唯一后缀：

```text
public-games/{平台}/{英文标识}/rom/{原始文件名}
public-games/{平台}/{英文标识}/cover/{封面文件名}
public-games/{平台}/{英文标识}/screenshots/{截图文件名}
```

不需要手动在 VPS 的项目目录中创建这些文件夹，也不要把 ROM 放入 `client/public`。对象存储返回的 `/manus-storage/...` URL 会被游戏库使用。

## 文件限制

游戏文件最大 30 MB；封面最大 5 MB；单张截图最大 8 MB。游戏文件支持 `.nes`、`.gba`、`.wsc`、`.iso`、`.cue`、`.bin`、`.cso` 和 `.pbp`；`.wsc` 属于 WSC，不属于 GBA。封面与截图推荐使用 WebP，也接受 PNG/JPEG。封面推荐 512×512 或 600×800；截图推荐 16:9。格式校验和大小校验同时在前端和服务端执行。

## 当前运行边界

上传完成后，游戏条目会进入“我的游戏库”，可按平台筛选并选择查看元数据。当前 Babylon 原创原型可以直接游玩；GBA、土星 SS 和 PSP 的真实模拟器核心仍需分平台接入，未接入核心的上传条目会显示为“服务器已收录”，不会误启动其他游戏。
